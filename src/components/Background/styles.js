import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  container: {
    flex: 1,
    position: 'absolute',
    top: 0,
    width: '100%',
    height: '100%',
    zIndex: -1,
    backgroundColor: '#fff',
  },
  backgroundImage: {
    width: '100%',
    height: '60%',
  },
});
